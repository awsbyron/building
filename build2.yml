version: 0.2

phases:
  build:
    commands:
      - ls Data || true
      - readlink Data || true
      - system_profiler SPHardwareDataType | grep "Hardware UUID"
      - cat Data/test.txt || true

cache:
  paths:
    - 'Data/**/*'
